# Firebase Admin SDK Configuration
# These values are required for the Express API to connect to Firebase with admin privileges
# Get these from your Firebase project service account:
# Firebase Console > Project Settings > Service Accounts > Generate New Private Key

# Option 1: Use environment variables (recommended for production)
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your_project_id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

# Option 2: Use Application Default Credentials
# If running on GCP (Cloud Run, Cloud Functions, etc.), you can omit the above variables
# and the SDK will automatically use Application Default Credentials

# Server Configuration
PORT=3333
